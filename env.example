# Configurações de Desenvolvimento/Produção

# Chave secreta do Django (gere uma nova para produção)
SECRET_KEY=django-insecure-_9z8)nmq^y^msppxpa%iav6*obf83zgb!x%q8!t)$ges706*v6

# Debug - True para desenvolvimento, False para produção
DEBUG=True

# Hosts permitidos (separados por vírgula)
ALLOWED_HOSTS=localhost,127.0.0.1

# URL do banco de dados (será fornecida pelo Railway automaticamente)
# DATABASE_URL=postgresql://user:password@host:port/database

# Configurações opcionais de email (para produção)
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=seu-email@gmail.com
# EMAIL_HOST_PASSWORD=sua-senha-de-app 